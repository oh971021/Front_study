"use strict";

console.log("------------");

// 논리 연산자 : >, >=, <, <=, ==, !=

// 결합 연산자 : &&, ||, !

/* 삼항 연산자 : 둘 중 하나 값 출력
    - 조건식 ? true : false */

// 나이가 10살 
let age = 5;
let result = (age > 10) ? "welcome" : "go home";

console.log(result);
console.log(typeof(result));

